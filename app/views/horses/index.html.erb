<div class="container">
  <h1 class="mt-5">Nos chevaux</h1>
  <div class="horse-card-grid mx-auto mt-5">
      <% @horses.each do |horse| %>
        <div class="horse-card">
          <%= cl_image_tag horse.photo.key, class: "img-fluid" %>
          <div class="d-flex justify-content-between align-items-end p-2">
            <div class="horse-card-details">
              <h3><%= horse.name.upcase %></h3>
              <div class="breed">
                🐴 <%= horse.breed.upcase %>
              </div>
              <div class="price">
                🪙 <%= horse.price %>€ / J
              </div>
            </div>
            <%= link_to "Voir", horse_path(horse), class: "btn-pmured" %>
          </div>
        </div>
      <% end %>
  </div>
</div>
